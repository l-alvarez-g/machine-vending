{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "hostname": "0.0.0.0",  // <--- AÑADE ESTA LÍNEA CRÍTICA
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}/machine-service"
            },
            "ignore": [
                "**/vendor/**/*.php"
            ]
        }
    ]
}